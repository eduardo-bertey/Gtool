extends Button


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:

	pass


func _on_mouse_entered() -> void:
	$"../../TextEdit".text = " itorrent es un id buscado por trackers para buscar peer e identificarce , 
usa tecnicas de torrent usando hash sha256 y un identificador tambien puede hacer busquedas kadmelia"
	pass # Replace with function body.
